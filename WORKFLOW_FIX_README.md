# دليل إصلاح مشكلة جدولة التحديثات

## 📋 ملخص المشكلة

كان الموقع لا يقوم بجلب الألعاب الجديدة تلقائياً كل 6 ساعات كما هو مفترض.

## ✅ الإصلاحات المطبقة

### 1. تحسين ملف GitHub Actions Workflow

تم تحديث `.github/workflows/update.yml` بالتحسينات التالية:

- ✅ **إضافة Timeouts**: لكل خطوة timeout محدد لتجنب التعليق
- ✅ **معالجة الأخطاء**: استخدام `continue-on-error` للسكريبتات
- ✅ **فصل الخطوات**: فصل Steam و Epic إلى خطوات منفصلة
- ✅ **التحقق من التغييرات**: خطوة للتحقق قبل الـ commit
- ✅ **رسائل واضحة**: رسائل عربية واضحة في الـ logs

### 2. الجدولة

الـ Workflow يعمل على:
- **تلقائياً**: كل 6 ساعات (0، 6، 12، 18 UTC)
- **يدوياً**: عبر زر "Run workflow" في GitHub Actions

## 🔧 خطوات التفعيل

### 1. تفعيل GitHub Actions

انتقل إلى إعدادات المستودع على GitHub:

```
Settings → Actions → General
```

تأكد من:
- ✅ تفعيل "Allow all actions and reusable workflows"
- ✅ تفعيل "Read and write permissions" في Workflow permissions

### 2. تشغيل يدوي للتأكد

1. اذهب إلى تبويب **Actions** في المستودع
2. اختر **Update Free Games** من القائمة اليسرى
3. اضغط على **Run workflow**
4. راقب التنفيذ للتأكد من عدم وجود أخطاء

### 3. مراقبة التنفيذ التلقائي

- سيتم تنفيذ الـ Workflow تلقائياً كل 6 ساعات
- يمكنك مراقبة السجلات في تبويب Actions

## 📊 ما الذي يحدث عند كل تحديث؟

1. **جلب ألعاب Steam** (timeout: 10 دقائق)
2. **جلب ألعاب Epic** (timeout: 10 دقائق)
3. **تحديث الوقت** (timestamp)
4. **التحقق من وجود تغييرات**
5. **Commit و Push** (فقط إذا كانت هناك تغييرات)

## ⚠️ ملاحظات مهمة

- GitHub Actions قد لا ينفذ cron jobs بدقة 100% في الوقت المحدد
- قد يكون هناك تأخير يصل إلى 15 دقيقة
- إذا فشل أحد السكريبتات، سيستمر الـ Workflow في تنفيذ الباقي
- إذا لم تكن هناك ألعاب جديدة، لن يتم إنشاء commit

## 🐛 استكشاف الأخطاء

### المشكلة: Workflow لا يعمل

**الحل:**
1. تحقق من تفعيل GitHub Actions في الإعدادات
2. تحقق من صلاحيات الـ Workflow
3. جرب التشغيل اليدوي للتأكد

### المشكلة: فشل في الـ Commit

**الحل:**
1. تحقق من صلاحيات "Read and write permissions"
2. تحقق من سجلات الأخطاء في Actions

### المشكلة: لا توجد ألعاب جديدة

**الحل:**
- هذا طبيعي! ليس دائماً هناك ألعاب جديدة
- الـ Workflow سيسجل "لا توجد تغييرات جديدة"

## 📝 التحقق من آخر تحديث

يمكنك التحقق من آخر تحديث عبر:

1. **ملف `update_timestamp.json`**: يحتوي على وقت آخر تحديث
2. **تبويب Actions**: يعرض سجل جميع التنفيذات
3. **Commits**: كل تحديث يُنشئ commit بتاريخ ووقت

## 🎯 النتيجة المتوقعة

بعد تطبيق هذه الإصلاحات:
- ✅ الموقع يتحدث تلقائياً كل 6 ساعات
- ✅ معالجة أفضل للأخطاء
- ✅ سجلات واضحة وسهلة القراءة
- ✅ لا commits فارغة
